apiVersion: v1
kind: Service
metadata:
  name: {{ include "appstudio.fullname" . }}
  labels:
    {{- include "appstudio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "appstudio.selectorLabels" . | nindent 4 }}
