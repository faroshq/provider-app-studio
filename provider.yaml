# Provider — declarative provisioning record (admin.faros.sh, admin-only).
# The hub's Provider controller creates root:faros:providers:app-studio, its
# "provider" ServiceAccount, and the app-studio-kubeconfig Secret in
# root:faros:system:providers. The APIExport/schemas/bind-grant come from app-studio's
# own `init`; routing/UI from the companion CatalogEntry (Helm-rendered from
# deploy/chart/templates/catalogentry.yaml).
apiVersion: admin.faros.sh/v1alpha1
kind: Provider
metadata:
  name: app-studio
spec:
  displayName: "App Studio"
